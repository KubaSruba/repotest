/*
 * Strinigi.cpp
 *
 *
 *
 */


#include <iostream>
#include <string>

using namespace std;

int main(int argc, char **argv)
{   //char teksty[120];
    string tekst, szyfrogram, odszyfrowany;
    cout << "Poradj tekst do zaszyfrowanai: ";
    cin >> tekst;

    cout << tekst << endl << "Rozmiar: " << tekst.size() << endl; {
    for (int i = 0; i < tekst.size(); i++)
        cout << tekst[i] << " ";
        szyfrogram[i] = zakodowany_znak;
    }
	return 0;
}

